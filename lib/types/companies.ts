export type Company = {
  slug: string
  title: string
  tagline: string
  shortDescription: string
  longDescription: string
  gallery: string[]
  tags: string[]
  liveUrl?: string
  githubUrl?: string
}
